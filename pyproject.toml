[project]
name = "transcribe-skill"
version = "0.0.1"
description = "薄、能自愈的 agent skill：把任意视频/音频源转录成文本并清洗。"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
]

[tool.uv]
# yt-dlp 和 mlx-whisper 走 `uv tool install`，不进项目依赖。
